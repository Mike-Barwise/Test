# Hello World in Rust

A simple hello world example written in Rust.

## Building and Running

### Run directly
```bash
cargo run
```

### Build binary
```bash
cargo build
```

### Build optimized release
```bash
cargo build --release
```

The compiled binary will be located in `target/debug/hello-world` (or `target/release/hello-world` for release builds).
